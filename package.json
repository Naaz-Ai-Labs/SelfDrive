{
  "name": "darshan-tours",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "crm",
    "web"
  ],
  "scripts": {
    "build": "npm run build --prefix crm",
    "build:crm": "npm run build --prefix crm",
    "build:web": "npm run build --prefix web"
  }
}
